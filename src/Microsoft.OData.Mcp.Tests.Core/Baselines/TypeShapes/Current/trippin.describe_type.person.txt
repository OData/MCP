Person  (set: People, key: UserName)
  // enum literal: Trippin.PersonGender'Male'
  UserName: string // key
  FirstName: string
  LastName?: string
  MiddleName?: string
  Gender: enum(Male|Female|Unknown)
  Age?: int64
  Emails?: string[]
  AddressInfo?: Location[]
  HomeAddress?: Location
  FavoriteFeature: enum(Feature1|Feature2|Feature3|Feature4)
  Features: enum(Feature1|Feature2|Feature3|Feature4)[]
  Friends -> Person[]
  BestFriend? -> Person
  Trips -> Trip[]
  operations
    GetFavoriteAirline() -> Airline
    GetFriendsTrips(userName: string) -> Trip[]
    UpdateLastName(lastName: string) -> bool // writes
    ShareTrip(userName: string, tripId: int) // writes